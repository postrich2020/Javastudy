
* 콜렉션 (표준화)
			- 기본적인 자료구조(개념)를 만들어 줌.
			- java.util 패키지
			
	1. List
			- ArrayList : Object[] 동적크기
			- 순서저장(index 보유)
	
	2. set
			- 순서없음(index 없음)
			- 중복값 저장 안됨
	
	3. Map (interface임)
			- 키(key)와 밸류(value) 저장 구조
			- 키는 중복 안됨(같은 키 값의 경우, 먼저 입력된 키 값이 나중 입력된 키 값으로 대체된다.) 